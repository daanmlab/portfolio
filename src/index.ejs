<!DOCTYPE html>
<html lang="en" style="background-color: #000">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, viewport-fit=cover"
    />

    <title>portfolio</title>
    <link rel="shortcut icon" type="image/svg" href="favicon.svg" />
  </head>
  <style>
    html {
      --overlay-bg: #fff !important;
    }

    .background--custom {
      background-color: #ffffff;
      width: 120vw;
      height: 120vh;
      position: fixed;
      overflow: hidden;
      z-index: -2;
      top: -10vh;
      left: -10vw;
    }
    canvas#Canvas {
      z-index: -1;
      position: absolute;
      width: 100%;
      height: 100%;
      transform: rotate(0deg) scale(1.5) translateY(0%);

      --gradient-color-1: #f72585;
      --gradient-color-2: #7209b7;
      --gradient-color-3: #4361ee;
      --gradient-color-4: #4cc9f0;
      --gradient-speed: 0.000002;
    }
  </style>
  <body style="height: 100vh; width: 100vw; opacity: 0">
    <div class="background--custom">
      <canvas id="Canvas"></canvas>
    </div>
    <main>
      <section id="Hello" class="hero is-transparent is-fullheight">
        <div class="hero-body">
          <div class="container has-text-centered">
            <h1 class="title"><%= name %></h1>
            <p class="subtitle">This is what I do.</p>
          </div>
        </div>
        <div class="scrolldown-wrapper">
          <div class="scrolldown">
            <svg height="30" width="10">
              <circle class="scrolldown-p1" cx="5" cy="15" r="2" />
              <circle class="scrolldown-p2" cx="5" cy="15" r="2" />
            </svg>
          </div>
        </div>
      </section>
      <section id="Projects">
        <div class="container has-text-centered">
          <p class="title is-size-3">Projects</p>
        </div>
        <% projects.forEach(function(project){ %> <%- include
        ./components/project.ejs -%> <% }); %>
      </section>
      <section id="Socials">
        <div class="container has-text-centered">
          <p class="title is-size-3">Socials</p>
        </div>

        <div class="container has-text-centered">
          <% socials.forEach(function(social){ %>
          <a class="social--button" href="<%- social.href -%>">
            <img data-src="<%= social.icon %>" alt="<%= social.icon %>" />
          </a>
          <% }); %>
        </div>
      </section>
    </main>
  </body>
</html>
